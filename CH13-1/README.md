# 실습 과제 1 
-배열명의 의미를 설명하라.
>배열 첫 번째 요소의 주소

-배열표현과 포인터(주소) 표현의 변환 공식을 설명하라.
> | 배열 | 포인터(주소) |
  |---|---|
  | a  | &a[0] |
  | a[l]  | *(a+l)  |
  | &a[l] | a+l  |

-배열의 첫번째 요소의 주소만 알면 모든 원소의 주소를 계산할 수 
있다. 방법을 설명하시오.
>배열은 연속으로 할당되는 특성이 있다. 따라서 요소의 주소를 알면 요소의 바이트 수 만큼 첫 번째에 더하여 두 번째 주소를 알 수 있고 이를 반복하면 배열의 모든 주소를 알 수 있다.

-배열의 첫번째 요소의 주소만 알면 모든 원소의 값를 계산할 수 있
다. 방법을 설명하시오.
>위 질문과 마찬가지로 모든 배열의 주소를 찾을 수 있게 되므로 원하는 배열에 값도 포인터를 활용하여 알 수 있게 된다.

-포인터에 대해 가능한 연산의 종류와 의미를 설명하라.
> 간접참조(*), 정수덧셈(+), 정수뺄셈(-), 증감(++, --)이다

-포인터 연산을 기존의 산술연산과 다르게 정의한 이유는 무엇일까
>가장 대표적인 예시로 변수 a에 1을 더하면 a+1의 값이 산출되겠지만 포인터에서는 값에 1을 더하는 것이 아니라 다음 번지의 값을 불러오게 되므로 다르게 정의한다

# 실습 과제 2
## 코드 설명
-int grade[5];
> 배열 5개 선언

-int sum = 0;
> 합 값 0으로 초기화

-int* p = grade;
> 포인터 p는 성적의 주소

-for (int i = 0; i < 5; i++)
> for 반복문 i는 0으로 초기화, 5보다 작을 때까지 1을 더하고 재시행

-printf("성적을 입력하시오: ");
> 성적을 입력하시오 출력
    
-scanf("%d", p + i);
>  입력 받은 숫자 p+i에 저장
    
-sum += *(p + i);
>합은 p+i의 값

-printf("성적 평균 = %.1f\n", sum / 5.0);
>성적 평균 출력 합을 5로 나눔으로써 표현

# 실습 과제 2-1
## 실행 값
-<img width="498" height="273" alt="image" src="https://github.com/user-attachments/assets/174664e5-c67a-4c9f-82b8-33d8933f6305" />

# 실습 과제 3
## a++, ++a의 차이
-a++는 값에다가 증가한 후 사용하고 ++a 증가한 후 값을 사용한다. 즉 배열에서 사용했을 때 첫 번째 칸의 값을 찾으려고 했으나 ++a를 사용하면 다음 칸의 값을 알게 된다.

## 코드의 문제와 값
-a는 배열이므로 즉 상수이다. 증감을 할 수 없다. 

-<img width="519" height="190" alt="image" src="https://github.com/user-attachments/assets/844d7ca7-02cf-47c1-b588-c651a06ad900" />

# 실습 과제 4
-double arr[5];
> 실수형 배열 5개 선언

-double* ptr = arr;
> 실수형 포인터는 arr배열 주소

-for (int i = 0; i < 5; i++)
> for 반복문 i는 0초기화 i가 5보다 작을 때 1씩 더하며 반복

-printf("숫자를 입력하세요: ");
> 숫자를 입력하시오 출력

-scanf(" %lf", &arr[i]);
> 입력 받은 숫자 배열 값에 반복하여 저장

-for (int j = 0; j < 5; j++)
> for 반복문 j는 0초기화 j가 5보다 작을 때 1씩 더하며 반복

-*ptr+= 2;
> 포인터 ptr 즉 배열의 값 2 증가

-ptr++;
> ptr 즉 arr 주소 증감 연산자 활용 1 증가

-ptr = arr; 
> ptr은 배열 arr 값 초기화

-printf("\n");
> 줄 간걱 띄움 출력

-for (int i = 0; i < 5; i++)
> for 반복문 i는 0초기화 i가 5보다 작을 때 1씩 더하며 반복
  
-printf("%.1f, ", *ptr);
> 출력 함수 포인터 ptr 출력

-ptr++;
> ptr 증감 연산자 활용 1씩 증가 시키면서 반복

# 실습 과제 4-1
-<img width="602" height="268" alt="image" src="https://github.com/user-attachments/assets/5201694f-9c95-4fd9-8333-9d45e825ba4e" />

# 실습 과제 5
-double arr[5];
> 실수형 배열 5개 선언

-double* ptr = arr;
> 실수형 포인터는 배열 요소의 주소 초기화

-for (int i = 0; i < 5; i++)
> for 반복문 i는 0초기화 i가 5보다 작을 때 1씩 더하며 반복

-printf("숫자를 입력하세요: ");
> 숫자를 입력하시오 출력
    
-scanf(" %lf", &arr[i]);
> 배열의 값에 실수 저장

-printf("\n1번 결과: ");
> 출력함수 줄 간격하고 결과: 출력

-for (int i=0; i<5; i++)
> for 반복문 i는 0초기화 i가 5보다 작을 때 1씩 더하며 반복

-arr[i] += 2;
> 반복문 안에서 배열 표현으로 배열 값 2 증가 초기화

-printf("%.2f ", arr[i]);
> 배열 소수점 아래 2자리까지 출력		

-printf("\n2번 결과: ");
> 줄 간격 띄우고 2번 결과 출력

-for (int i = 0; i < 5; i++)
> for 반복문 i는 0초기화 i가 5보다 작을 때 1씩 더하며 반복

-ptr[i] += 2;
> 포인터 변수를 활용한 값 2증가

-printf("%.2f ", arr[i]);
> 배열 소수점 아래 2자리까지 출력

-printf("\n3번 결과: ");
> 줄 간격 띄우고 3번 결과 출력

-for (int i = 0; i < 5; i++)
> for 반복문 i는 0초기화 i가 5보다 작을 때 1씩 더하며 반복

-*(ptr+i) += 2;
> 포인터 주소를 활용한 값 2 증가 표현

-printf("%.2f ", arr[i]);
> 배열 소수점 아래 2자리까지 출력

-printf("\n4번 결과: ");
> 줄 간격 띄우고 4번 결과 출력

-for (int i = 0; i < 5; i++)
> for 반복문 i는 0초기화 i가 5보다 작을 때 1씩 더하며 반복

-*(arr + i) += 2;
> 포인터 변수를 활용한 깂 2 증가 주소 표현

-printf("%.2f ", arr[i]);
> 배열 소수점 아래 2자리까지 출력

-printf("\n5번 결과: ");
> 줄 간격 띄우고 5번 결과 출력

-for (int i = 0; i < 5; i++)
> for 반복문 i는 0초기화 i가 5보다 작을 때 1씩 더하며 반복

-*ptr++ += 2;
> 증감연산자를 활용한 값 2증가 표현

-printf("%.2f ", arr[i]);
> 배열 소수점 아래 2자리까지 출력

# 실습 과제 5-1
-<img width="711" height="346" alt="image" src="https://github.com/user-attachments/assets/e5657134-52d0-4c95-818c-98083ed62baf" />

